apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: assessment-efs-claim
  namespace: assessment
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 5Gi
